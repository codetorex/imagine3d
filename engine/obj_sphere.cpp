#include "cobject.h"
#include "constant.h"

/*
void IObject::Sphere(vector3f cords,float radius,float segs)
{
	float gens = 360 / segs;
	for (float teta=0;teta<360;teta+=gens)
	{
		for(float phi=0;phi<360;phi+=gens)
		{
			x = cos(a)*cos(a);
			y = cos(a)*sin(a);
			z = sin(a);
		}
	}
}
*/