//#include "stdafx.h"
#include "cbitmap.h"









/*float dtnp(int x,int y,float* xco,float* yco,int pc); // distance to nearest point
float dtnp(int x,int y,float* xco,float* yco,int pc)
{
	float dist;
	float mind=999999999;

	for (int i=0;i<pc;i++)
	{
		dist = sqrt( (xco[i]-x)*(xco[i]-x) + (yco[i]-y)*(yco[i]-y));
		if (dist < mind) mindist = dist;
	}
	return mindist;
}*/

